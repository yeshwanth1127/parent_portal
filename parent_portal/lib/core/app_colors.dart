import 'package:flutter/material.dart';

/// Rainbow color palette inspired by vibrant illustration reference:
/// deep blues, purples, pinks, fuchsias, oranges, yellows.
class AppColors {
  AppColors._();

  // Deep night / background
  static const Color deepBlue = Color(0xFF1a1a2e);
  static const Color darkIndigo = Color(0xFF16213e);

  // Purple spectrum (mushroom caps, primary)
  static const Color violet = Color(0xFF7c3aed);
  static const Color purple = Color(0xFFa855f7);
  static const Color purpleLight = Color(0xFFc084fc);

  // Pink / fuchsia (stems, accents)
  static const Color fuchsia = Color(0xFFd946ef);
  static const Color pink = Color(0xFFec4899);
  static const Color pinkLight = Color(0xFFf472b6);

  // Warm (glows, highlights)
  static const Color orange = Color(0xFFf97316);
  static const Color amber = Color(0xFFfbbf24);
  static const Color yellow = Color(0xFFfacc15);

  // Neutrals for form side
  static const Color surfaceLight = Color(0xFFfafafa);
  static const Color textDark = Color(0xFF1f2937);
  static const Color textMuted = Color(0xFF6b7280);
}
